
{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && live-server ."
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "live-server": "^6.0.0"
  }
}

